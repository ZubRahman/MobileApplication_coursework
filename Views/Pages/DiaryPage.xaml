<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyMauiApp.Views.Pages.DiaryPage"
             Title="Diary">

    <VerticalStackLayout Padding="10">

        <Label Text="My Diary" FontSize="22" FontAttributes="Bold" />

        <CollectionView ItemsSource="{Binding Entries}">
            <CollectionView.EmptyView>
                <Label Text="No diary entries yet. Rate a game to add one."
                       Padding="10" />
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="6">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditEntryCommand}"
                                CommandParameter="{Binding .}" />
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding GameTitle}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding Rating, StringFormat='Rating: {0}/10'}" FontSize="13" />
                            <Label Text="{Binding CreatedAt, StringFormat='Saved: {0:dd MMM yyyy HH:mm}'}" FontSize="12" />
                            <Label Text="{Binding Review}" FontSize="12" LineBreakMode="TailTruncation" MaxLines="2"/>
                        </VerticalStackLayout>
                    </Frame>
                    
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>